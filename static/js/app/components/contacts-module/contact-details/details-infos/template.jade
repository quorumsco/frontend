details-infos
  tab-menu(id="{{id}}" tab="1")
  each val in ["surname", "firstname", "email"]
    info-field #{val} :
      input(v-model="contact.#{val}" type="text" placeholder="#{val}" value="{{contact.#{val}}}")