contact-details
  tab-menu(id="{{id}}" tab="{{tab}}" nbTabs="{{nbTabs}}" nbNotes="{{nbNotes}}" v-class="hidden: !showTabs")
  component(is="{{view}}" contact="{{contact}}" id="{{id}}" keep-alive)

  //- details-tags
  //-   tag-list
  //-     list-item NPA
  //- details-notes
  //-   note-list
  //-     list-item This is a note
